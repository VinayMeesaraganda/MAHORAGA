/** Frozen research choices. This module cannot place orders or select the active strategy. */
export const GUIDANCE_PROFILE = Object.freeze({
  name: "guidance-continuation-v1",
  revision: "2026-09-12-1",
  mode: "shadow",
  revenueConsensusRequired: false,
  riskFraction: 0.00125,
  nameFraction: 0.05,
  grossFraction: 0.25,
  sectorFraction: 0.1,
  portfolioRiskFraction: 0.00625,
  maxPositions: 5,
  maxSectorPositions: 2,
  drawdownPause: 0.03,
  minPrice: 10,
  minAdv20: 50_000_000,
  maxSpreadBps: 50,
  atrMultiple: 2.5,
  minStopFraction: 0.015,
  maxStopFraction: 0.08,
  holdSessions: 10,
  entryMinute: 605,
  entryEndMinute: 610,
  quoteAgeMs: 30_000,
  newsAgeMs: 300_000,
  calendarAgeMs: 86_400_000,
  blackoutMs: 30 * 60_000,
  entryTimeoutMs: 60_000,
  costScenariosBps: [10, 25, 50],
} as const);
